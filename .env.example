# Railway Redis connection URL. Use a persistent Redis service with noeviction.
REDIS_URL=redis://default:password@127.0.0.1:6379

# A dedicated read-only MySQL user is recommended. The monitor only runs SELECT 1.
DB=mysql://uptime:password@127.0.0.1:3306/melonis_wiki

# Railway supplies PORT automatically.
# PORT=3000
